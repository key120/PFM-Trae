# 项目部署指南 (Vercel)

本指南将指导您如何将 PFM-Trae 项目的前端部分部署到 Vercel 平台。

## 1. 准备工作

### 1.1 代码提交
确保您所有的代码修改都已经提交（Commit）并推送（Push）到您的 Git 仓库（GitHub、GitLab 或 Bitbucket）。

### 1.2 获取环境变量
您需要准备好 Supabase 的环境变量，这些可以在项目的 `web/.env` 文件中找到：
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

> **注意**：请勿将 `.env` 文件提交到公共代码仓库。Vercel 会在部署过程中通过安全的方式注入这些变量。

### 1.3 路由配置 (已自动完成)
为了支持 React 单页应用 (SPA) 的路由跳转，我们已经在 `web` 目录下添加了 `vercel.json` 配置文件。您无需进行额外操作。

---

## 2. Vercel 部署步骤

### 2.1 登录 Vercel
1. 访问 [https://vercel.com](https://vercel.com)
2. 使用您的 GitHub、GitLab 或 Bitbucket 账号登录。

### 2.2 导入项目
1. 在 Vercel 控制台首页（Dashboard），点击 **"Add New..."** -> **"Project"**。
2. 在 **"Import Git Repository"** 列表中，找到您的 `PFM-Trae` 仓库，点击 **"Import"**。

### 2.3 配置项目 (关键步骤)
在 "Configure Project" 页面，请按照以下说明进行配置：

1. **Project Name**: 可以保持默认，或修改为您喜欢的名字（如 `pfm-trae-web`）。
2. **Framework Preset**: Vercel 通常会自动检测为 `Vite`。如果没有，请手动选择 **Vite**。
3. **Root Directory** (非常重要):
   - 点击 "Root Directory" 旁边的 **"Edit"** 按钮。
   - 在弹出的文件树中，选择 **`web`** 文件夹。
   - 点击 **"Continue"**。
   *原因：我们的前端代码位于仓库的 `web` 子目录下。*
4. **Build and Output Settings**: 
   - 保持默认即可。
   - Build Command: `npm run build` (或 `tsc -b && vite build`)
   - Output Directory: `dist`
   - Install Command: `npm install`
5. **Environment Variables**:
   - 展开 "Environment Variables" 部分。
   - 逐个添加以下变量（从 `web/.env` 文件中复制值）：
     - Key: `VITE_SUPABASE_URL`
     - Value: `您的_SUPABASE_URL`
     - 点击 **"Add"**
     - Key: `VITE_SUPABASE_ANON_KEY`
     - Value: `您的_SUPABASE_ANON_KEY`
     - 点击 **"Add"**

### 2.4 点击部署
确认以上信息无误后，点击底部的 **"Deploy"** 按钮。

---

## 3. 验证部署

部署过程通常需要 1-2 分钟。

1. **等待构建完成**：您会看到构建日志在滚动。
2. **部署成功**：当看到满屏的彩带庆祝画面时，说明部署成功！
3. **访问网站**：点击预览图或 **"Visit"** 按钮，访问您的线上网站。
4. **测试功能**：
   - 尝试注册/登录（验证 Supabase 连接）。
   - 上传文档并预览（验证核心功能）。
   - 刷新页面（验证路由配置是否生效）。

## 4. 后续更新

以后每当您将代码推送到 Git 仓库的 `main` 分支时，Vercel 会自动触发新的部署，您无需重复上述步骤。

---

**常见问题排查：**
- **404 Not Found**: 如果在刷新页面时出现 404，请检查是否已将 `vercel.json` 提交到仓库，或者 Vercel 配置中的 "Rewrites" 是否正确。
- **构建失败**: 请检查 Build Logs，通常是因为类型错误（TypeScript Error）或依赖缺失。确保本地运行 `npm run build` 能成功。
