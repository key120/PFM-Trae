# 项目开发计划文档

基于《项目文档管理网站需求文档》制定。

## 开发规则（强制）

1. 每个子任务完成后，必须先完成对应的功能测试或单元测试并记录结果；测试通过后停止在该子任务边界，明确询问“是否继续下一子任务”；得到同意后再进入后续子任务开发。
2. 每个阶段开始前，请确认当前代码状态。
3. 每个阶段完成后，请运行相关验证或单元测试 (使用 Chrome DevTools 或 Jest/Vitest)。
4. 每个阶段完成后，更新对应文档与验收记录，确保需求与实现一致。
5. 开发完成的任务必须在本文件对应条目的中括号内更新状态（类似 TODOList）：完成后将 `- [ ]` 改为 `- [x]`。

## 1. 项目初始化 (Project Initialization)

- [ ] **环境搭建**：
  - 使用 React + TypeScript 初始化项目 (推荐使用 Vite 以获得更好的开发体验)。
  - 配置 ESLint + Prettier 代码规范。
  - 初始化 Git 仓库。
- [ ] **依赖安装**：
  - 安装 UI 库：Ant Design。
  - 安装状态管理：Zustand。
  - 安装工具库：Axios, JSZip, file-saver, lodash, classnames 等。
  - 安装文档处理库：docx-preview, mammoth.js, docx。
- [ ] **基础架构**：
  - 搭建目录结构 (components, stores, utils, services, pages, types)。
  - 配置路由 (React Router)。
- [ ] **阶段验证**：
  - 确保项目可正常启动。
  - 确保 Lint 检查通过。

## 2. 第一阶段：UI 框架与用户认证 (Phase 1: UI & Auth)

- [ ] **UI 布局开发**：
  - 开发全局 Layout（顶部导航栏、左侧目录区容器、右侧预览区容器）。
  - 实现响应式布局结构。
- [ ] **认证功能开发** (基于 Supabase)：
  - 集成 Supabase 客户端。
  - 开发登录页面 (Login Page)。
  - 开发注册页面 (Register Page)。
  - 实现 Auth Store (Zustand) 管理用户状态。
  - 实现路由守卫 (Protected Routes)。
- [ ] **阶段验证**：
  - 测试注册、登录流程。
  - 测试未登录状态下的页面访问限制。

## 3. 第二阶段：文档上传与基础预览 (Phase 2: Upload & Preview)

- [ ] **上传组件开发**：
  - 实现文件拖拽上传区域。
  - 实现文件格式校验 (.docx) 和大小限制 (50MB)。
  - 实现上传进度条 UI。
- [ ] **文档预览引擎**：
  - 集成 `docx-preview` 库。
  - 实现 `DocumentPreview` 组件。
  - **核心特性**：实现文档中的"样式映射系统" (StyleMapper)，优化渲染效果，将 Word 样式映射为 CSS 类。
- [ ] **阶段验证**：
  - 上传标准 DOCX 文件，验证是否能成功渲染。
  - 验证样式映射是否生效（标题、段落样式是否符合预期）。

## 4. 第三阶段：文档结构解析与目录生成 (Phase 3: Parsing & TOC)

- [ ] **文档解析核心**：
  - 实现 `HeadingExtractor` 类 (基于 mammoth.js 或 XML 解析)。
  - 提取 H1-H6 标题、层级关系及在文档中的位置索引。
- [ ] **目录树组件**：
  - 开发左侧目录树 UI。
  - 实现标题的层级缩进显示。
  - 实现目录项的展开/折叠。
- [ ] **阶段验证**：
  - 上传多层级文档，验证目录树生成是否准确。
  - 验证点击目录项是否能跳转/定位到预览区对应位置。
  - **单元测试**：针对 `HeadingExtractor` 编写测试用例，验证不同结构的文档解析结果。

## 5. 第四阶段：交互筛选与动态序号 (Phase 4: Interaction & Renumbering)

- [ ] **筛选逻辑开发**：
  - 实现目录树的复选框逻辑 (单选、多选、级联选择)。
  - 实现 `ContentFilter` 类：根据选中状态隐藏/显示预览区对应段落。
- [ ] **动态序号系统**：
  - 实现 `NumberingManager` 类：根据筛选结果重新计算章节序号。
  - 实时更新目录树和预览区中的序号显示 (DOM 操作或重新渲染)。
- [ ] **阶段验证**：
  - 测试取消勾选父标题时，子标题是否自动取消。
  - 测试隐藏章节后，预览区内容是否正确隐藏。
  - 测试隐藏章节后，后续章节序号是否正确重排。
  - **单元测试**：针对 `NumberingManager` 编写测试用例，验证各种筛选组合下的序号计算逻辑。

## 6. 第五阶段：文档导出 (Phase 5: Export)

- [ ] **DOCX 导出开发**：
  - 集成 `docx` 库。
  - 实现 `DocumentExporter` 类。
  - 根据当前筛选状态，重组文档内容并生成新的 DOCX 文件。
  - 确保导出文档保留原格式和新的序号。
- [ ] **阶段验证**：
  - 筛选部分章节后导出。
  - 打开导出的文档，检查内容是否完整、序号是否连续、格式是否错乱。

## 7. 第六阶段：性能优化与收尾 (Phase 6: Optimization & Polish)

- [ ] **性能优化**：
  - (可选) 引入 Web Worker 处理大文件解析。
  - (可选) 针对超长文档目录实现虚拟滚动。
- [ ] **UI 细节完善**：
  - 增加加载动画 (Loading spinners)。
  - 完善错误提示 (Toast notifications)。
  - 检查浏览器兼容性。
- [ ] **最终验收**：
  - 进行全流程测试 (从注册到导出)。

## 8. 第七阶段：历史版本、共享与客户端加密（路径A）(Phase 7: History, Sharing & E2EE)

> 目标：继续使用 Supabase 做鉴权与元数据（RLS），大文件放 Cloudflare R2；文档内容采用客户端加密存储；共享采用邀请制（指定用户/用户组），并实现只读/可编辑/可转发/可管理的角色体系；版本信息支持长期保留（仅载入最新版本）。

- [x] **数据库与权限模型（Supabase Postgres + RLS）**：
  - 设计并创建表结构（建议最少包含）：
    - users\_profile：存 display\_name 与公钥 public\_key（用于密钥分发）。
    - groups / group\_members：用户组与成员关系（支持组内角色）。
    - documents：文档元数据（可包含 encrypted\_title、deleted\_at）。
    - document\_members：文档-用户授权（role + status：invited/active/removed）。
    - document\_group\_members：文档-用户组授权（role）。
    - document\_keys：每用户一份的 wrapped\_document\_key（加密后的 DocumentKey）与 key\_version。
    - document\_versions：版本记录（r2\_key、content\_hash、encrypted\_meta、size\_bytes、expires\_at）。
    - audit\_logs（可选）：审计记录（分享、下载、上传、删除等）。
  - 为上述表补齐索引（document\_id、user\_id、expires\_at 等）与外键约束（保证一致性与可维护性）。
  - 编写并启用 RLS 策略：
    - 未登录无法访问任何业务表。
    - documents / document\_versions：仅文档成员（含通过组授权）可读；仅 editor/admin 可写版本；仅 admin 可删除版本/文档与管理成员。
    - document\_keys：仅 key 归属用户本人可读；仅 admin/inviter 可为目标用户写入 wrapped key（或通过受控 RPC 完成）。
    - groups / group\_members：组内可见；组 owner/admin 可管理成员与组内角色。
  - 以“角色能力”而非“页面入口”作为权限判断基准，确保前后端一致。
- [ ] **前端界面与交互（个人文档页签）**：
  - [x] **子任务 1：顶部栏按钮骨架（文档列表 / 保存）**
    - 在右上角头像左侧新增“文档列表”按钮（点击触发打开 Drawer）。
    - 在“导出文档”按钮左侧新增“保存”按钮：默认不显示，仅在导入或载入文档后显示。
    - 子任务验证：
      - 功能测试：未载入文档时不显示“保存”，载入后显示“保存”。
      - 功能测试：点击“文档列表”按钮能触发打开行为（可先用临时提示/空 Drawer 验证）。
  - [x] **子任务 2：文档列表 Drawer + Tabs 框架**
    - 实现从右侧滑出的文档列表 Drawer（支持打开/关闭、遮罩点击关闭、ESC 关闭）。
    - Drawer 内实现 Tabs：默认“个人文档”，另含“共享文档”（本阶段先展示占位文案，不实现功能）。
    - 子任务验证：
      - 功能测试：Drawer 打开/关闭逻辑正确，关闭后不残留遮罩/滚动锁异常。
      - 功能测试：Tabs 默认选中“个人文档”，切换到“共享文档”显示占位。
  - [x] **子任务 3：个人文档数据集成（DocumentService）**
    - 实现 `DocumentService`（或类似 API 层）封装 Supabase 查询。
    - 拉取当前用户个人文档列表（`owner_id = user.id`），并返回标准化的数据结构（含最新版本信息与展示字段）。
    - 处理 Loading / Error（错误提示与重试入口）。
    - 子任务验证：
      - 功能测试：无数据时展示空态；有数据时展示列表。
      - 功能测试：断网/鉴权失败时展示错误提示且不崩溃。
  - [x] **子任务 4：个人文档列表 UI（卡片展示）**
    - 在“个人文档”页签中以卡片列表展示：文档名称、版本号、时间、作者、备注、大小。
    - 每张卡片提供“载入”与“共享/取消共享”按钮（本阶段可先完成 UI 与状态展示，交互后续子任务逐步接入）。
    - 子任务验证：
      - 功能测试：卡片字段渲染完整，长文本截断/换行符合预期。
      - 功能测试：列表滚动与空态切换正常。
  - [x] **子任务 7：保存弹窗 UI（个人）**
    - （执行顺序调整）为便于在 UI 中真实调试个人文档卡片和“载入”行为，本子任务在实际开发中将提前于“版本号下拉”和“载入最新版本”执行。
    - 点击“保存”按钮打开“保存”弹窗。
    - 版本号输入框：系统自动填充默认值（例如 V1.0.0），用户可修改后再保存。
    - 备注输入框：默认为空。
    - 取消/关闭弹窗不产生任何数据变更。
    - 子任务验证：
      - 功能测试：默认值正确、可编辑；备注默认空；取消后列表与版本下拉不变化。
  - [x] **子任务 8：保存流程对接（生成版本 + 更新列表）**
    - （执行顺序调整）与子任务 7 一同提前，实现最小可用的“保存 → 写入 documents/document\_versions → 列表刷新”链路；后续 R2 预签名 URL 与端到端加密可按路径 A 要求逐步补齐细节。
    - **前置准备（Cloudflare R2）**：
      - 创建 R2 Bucket（建议按 env 区分，例如 `pfm-trae-dev-docs`），记录 bucket 名称与 account id。
      - 在 Cloudflare 中为该 Bucket 创建具备最小权限的 API Token（仅允许读写此 Bucket），拿到 access\_key / secret\_key。
      - 确认 R2 S3 兼容 Endpoint（形如 `https://<accountid>.r2.cloudflarestorage.com`），作为后续预签名 URL 生成的基础。
      - 在 Supabase 的Edge Function页面，以环境变量方式（Secret页签下）配置上述凭据与 Bucket 信息，禁止在前端或公开代码中直接出现；建议统一使用 `R2_ACCOUNT_ID`、`R2_BUCKET_NAME`、`R2_ACCESS_KEY_ID`、`R2_SECRET_ACCESS_KEY`、`R2_S3_ENDPOINT` 等环境变量命名，并在 Edge Function 中通过 `Deno.env.get(...)` 读取，严禁在代码中硬编码凭据。
    - 调用 `DocumentExporter` 生成 DOCX Blob。
    - 调用加密服务生成/获取 `DocumentKey` 并加密 Blob。
    - 获取 R2 PUT 预签名 URL 并上传加密内容。
    - 更新 Supabase `documents`（元数据）与 `document_versions` 表。
    - 保存多次仅更新同一条文档记录，但版本下拉可见新增版本（最新置顶）。
    - 未点击“保存”按钮时：文档在预览/编辑过程中的任何变更均不会写入文档列表记录（不自动保存）。
    - 子任务验证：
      - 功能测试：确认保存后列表刷新，版本下拉新增一条；取消保存不新增版本。
      - 功能测试：多次保存版本连续累积；卡片始终显示最新版本号。
  - [x] **子任务 5：版本号下拉（只读展示）**
    - 卡片默认显示最新版本号。
    - 点击版本号下拉展示所有历史版本信息（版本号、备注、作者、时间；最新版本在最上方）。
    - 下拉菜单仅只读展示，不提供任何交互操作入口。
    - 子任务验证：
      - 功能测试：下拉内容顺序正确；最新版本置顶且标识明显（如“最新”Tag）。
      - 功能测试：仅展示不触发载入/切换版本等行为。
  - [x] **子任务 6：载入最新版本到预览区（个人）**
    - 点击“载入”将对应个人文档的最新版本加载到文档预览区（不支持载入历史版本）。
    - 载入后同步更新左侧目录区（复用现有解析/目录生成能力）。
    - 预览区标题标识：载入后在标题后显示“（个人）”，蓝色字体。
    - 子任务验证：
      - 功能测试：点击“载入”后预览区与目录同步更新，滚动/目录定位仍可用。
      - 功能测试：仅载入最新版本；历史版本仅在下拉中可见但不可载入。
  - [x] **子任务 9：共享/取消共享按钮（个人文档卡片）**
    - 文档未共享时：按钮文案为“共享”，点击后打开共享设置弹窗（先完成 UI 与交互骨架）。
    - 文档已共享时：按钮文案为“取消共享”，点击后撤销共享状态；共享文档联动能力在后续子任务打通。
    - 子任务验证：
      - 功能测试：按钮文案随共享状态变化；弹窗打开/关闭正常；取消共享后状态回退正确。
- [x] **R2 接入与预签名 URL（最小权限 + 短时效）**：
  - [x] **子任务 0：R2 对象命名规范与清理策略**
    - 设计 r2\_key 模板，例如：`<project>/<env>/documents/<document_id>/<version_id>/<content_hash>.bin`：
      - `<project>`：固定为 `pfm-trae` 或类似项目代号。
      - `<env>`：环境标识，如 `dev` / `stg` / `prod`。
      - `<document_id>`：对应 `documents.id`，用于按文档维度批量清理。
      - `<version_id>`：对应 `document_versions.id` 或等价版本标识，用于按版本维度清理。
      - `<content_hash>`：加密后内容的 SHA-256 哈希（可截断为前 16～32 位十六进制），提升幂等与完整性校验能力。
    - 明确 r2\_key 与 `document_versions.r2_key` 的一一对应关系，仅由后端（Edge Function / Worker）生成，不接受前端自定义路径。
    - 定义清理策略：
      - 删除整篇文档时：以 `<project>/<env>/documents/<document_id>/` 为前缀，批量删除该文档下所有版本对象。
      - 删除单个版本时：以 `<project>/<env>/documents/<document_id>/<version_id>/` 为前缀，仅删除对应版本对象。
      - 幽灵数据治理：定期根据 `document_versions` 与 R2 对象列表比对，清理无对应版本记录的孤立对象，保证长期可运维性。
  - [x] **子任务 1：上传签名服务 API 设计与实现（PUT URL）**
    - 基于 Supabase Edge Function实现 `POST /r2/sign-upload`。
    - 入参包含 documentId/versionId、operation（upload）、contentHash、sizeBytes；返回 url、method、headers、expiresAt、r2Key。
    - 仅允许使用 PUT 写入指定 r2\_key，短时效（60～300 秒），同时限制 Content-Type 与最大 sizeBytes。
  - [x] **子任务 2：下载签名服务 API 设计与实现（GET URL）**
    - 实现 `POST /r2/sign-download`，入参包含 documentId + versionId 或 r2Key；返回 url、method、expiresAt。
    - 仅允许使用 GET 读取指定 r2\_key，短时效（60～300 秒），禁止前端自定义任意路径。
  - [x] **子任务 3：权限与安全约束实现**
    - 在签名服务中校验 Supabase JWT，并基于 RLS/角色判断是否允许生成签名（reader/editor/admin）。
    - 增加请求大小限制、R2 CORS 配置、一次性 nonce（可选）等防滥用与防重放措施。
  - [x] **子任务 4：审计与监控集成**
    - 在 audit\_logs 中记录签名生成、上传、下载等关键操作（包括 document\_id、user\_id、r2\_key、IP 等信息）。
    - 与第八阶段的审计/风控要求对齐，预留频率限制与告警入口。
  - [x] **子任务 5：R2 集成阶段验证**
    - 功能测试：上传/下载全流程打通，版本记录与 r2\_key 对应关系正确。
    - 权限测试：非成员或无权限角色无法获取预签名 URL。
    - 安全测试：URL 过期、路径篡改、重放等场景均被拒绝。
- [ ] **客户端加密存储（Web Crypto API，端到端加密）**：
  - [x] **子任务 0：加密架构与数据流确认**
    - 梳理“保存/载入”全链路的数据流：`DocumentKey` 生成与存储位置、`wrapped_document_key` 与 `document_keys` 表字段映射、`document_versions.r2_key` 与 R2 对象、`encrypted_meta` 中加密元数据（标题、选中目录等）。
    - 明确哪些字段在客户端加密（文档内容、文档标题、备注等），哪些字段以明文形式保留在数据库中用于查询和排序（如创建时间、拥有者 id 等）。
    - 与“R2 接入与预签名 URL”“版本记录与保留”“邀请制共享与角色管理”三块对齐约束，确保后续不会出现模型冲突。
  - [x] **子任务 1：用户密钥初始化（前置任务）**
    - 在用户注册/登录流程中集成密钥检查逻辑（登录后首次访问文档功能时触发也可）。
    - 若本地 IndexedDB 无私钥或服务端无公钥，自动生成 RSA-OAEP 密钥对。
    - 私钥存储于 IndexedDB（non-exportable），公钥上传至 `users_profile` 表；多端登录时保证“同一账号可拥有多把公钥”，后续通过 `document_keys` 表为每个公钥下发 `wrapped_document_key`。
    - 设计“密钥状态检测”接口：前端任意需要解密文档的地方都可以快速判断当前用户是否已完成密钥初始化。
  - [x] **子任务 2：定义密钥体系与生命周期**
    - 每文档生成独立的 `DocumentKey`（AES-GCM 256），仅在客户端内存中以明文形式存在，不持久化到任何后端或日志。
    - 用户持有 `UserKeyPair`（用于邀请制密钥分发）；公钥写入 `users_profile`，私钥仅本地保存（IndexedDB），禁止导出到服务器。
    - 通过 `wrapped_document_key` 方式将 `DocumentKey` 按用户分发：对每个被邀请用户使用其公钥封装文档的 `DocumentKey`，写入 `document_keys` 表，服务端只持有密文。
    - 定义 `DocumentKey` 生命周期与轮换策略：新增/移除成员、角色降级或疑似泄露时，如何触发生成新 `DocumentKey`、为仍有权限的成员重新分发，并与“版本记录与保留”策略对齐（例如仅保证最新版本可解密）。
  - [x] **子任务 3：文档加解密核心库（小文件）**
    - 基于 Web Crypto API 实现文档内容的加密/解密工具函数（不关心分块与网络，仅处理 `File/Blob <-> EncryptedBlob` 转换）。
    - 支持同时加密文档正文和与内容强相关的小型元数据（如文档标题、备注），并定义统一的 `EncryptedBlob` 头部描述字段（算法、版本号、metaHash 等）。
    - 设计错误码与异常行为：密文格式错误、MAC 校验失败、算法版本不支持时给出清晰的前端提示，避免泄露过多细节。
  - [x] **子任务 4：大文件分块加密/解密与上传下载链路**
    - 在核心库基础上实现大文件分块加密/解密：
      - 定义 `EncryptedBlob` 具体格式（header + chunks + tag/校验信息），记录算法、`chunkSize`、`chunks` 数量、`metaHash` 等。
      - 确保每块使用独立 nonce，防止 nonce 重用；对整体内容进行 SHA-256 完整性校验。
    - 与 R2 签名服务集成：
      - 上传：`DocumentExporter` 生成 DOCX Blob → 使用 `DocumentKey` 分块加密 → 调用 `/r2/sign-upload` 获取预签名 URL 和 `r2_key` → PUT 到 R2 → 在 `document_versions` 中写入 `r2_key` 与 `encrypted_meta`。
      - 下载：通过 `/r2/sign-download` 获取短时效 GET URL → 流式拉取并分块解密 → 还原为 `File` 再交给现有解析/预览流程。
    - 支持断点续传/失败重试（至少保证失败后可以重新上传同一版本而不会产生不一致数据）。
  - [x] **子任务 5：密钥备份与恢复策略（平台托管）**

    **5.1 架构设计与安全边界**
    - **平台托管模型定义**：
      - 后端使用 Master Key 加密存储用户 RSA 私钥的 JWK 格式
      - 明确声明：平台理论上具备解密用户文档的能力（非严格端到端加密���
      - 保持与现有 `UserKeyPair` / `DocumentKey` 接口兼容，不影响后续功能开发
    - **Master Key 管理**：
      - 使用 Supabase Vault 或环境变量存储 Master Key（AES-256-GCM 密钥）
      - Master Key 仅在 Edge Function 运行时可访问，前端无法获取
      - 定义 Master Key 轮换策略（手动触发，需重新加密所有备份）
    - **安全边界说明**：
      - 在用户协议/隐私政策中明确说明平台托管模式
      - 配合运维侧的访问审计、密钥管理和备份策略
      - 为未来演进预留扩展点（如改为客户端自管备份）

    **5.2 数据库设计**
    - **创建 `user_key_backups` 表**：
      ```sql
      CREATE TABLE user_key_backups (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
        encrypted_private_key TEXT NOT NULL,  -- Master Key 加密后的 JWK
        key_version INT NOT NULL DEFAULT 1,   -- 密钥版本号
        encryption_algorithm TEXT NOT NULL DEFAULT 'AES-256-GCM',
        nonce TEXT NOT NULL,                  -- 加密时使用的 nonce (base64)
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        UNIQUE(user_id, key_version)
      );

      CREATE INDEX idx_user_key_backups_user_id ON user_key_backups(user_id);
      CREATE INDEX idx_user_key_backups_key_version ON user_key_backups(user_id, key_version DESC);
      ```
    - **RLS 策略**：
      - 用户只能读取自己的备份记录（但无法解密，因为 Master Key 在后端）
      - 仅通过 Edge Function 的受控 RPC 进行写入和解密操作
      ```sql
      ALTER TABLE user_key_backups ENABLE ROW LEVEL SECURITY;

      -- 用户可读取自己的备份元数据（不含解密能力）
      CREATE POLICY “Users can view own backups”
        ON user_key_backups FOR SELECT
        USING (auth.uid() = user_id);

      -- 禁止直接写入，必须通过 Edge Function
      CREATE POLICY “No direct insert”
        ON user_key_backups FOR INSERT
        WITH CHECK (false);
      ```

    **5.3 后端 Edge Function 实现**

    **5.3.1 备份 API: `POST /key-backup`**
    - **功能**：接收前端提交的用户私钥 JWK，使用 Master Key 加密后存储
    - **入参**：
      ```typescript
      {
        privateKeyJwk: JsonWebKey,  // 用户 RSA 私钥的 JWK 格式
        keyVersion: number          // 密钥版本号（默认 1）
      }
      ```
    - **处理流程**：
      1. 验证 Supabase JWT，获取 user_id
      2. 从环境变量读取 Master Key（base64 编码的 AES-256 密钥）
      3. 生成随机 nonce (12 bytes)
      4. 使用 Web Crypto API 的 AES-GCM 加密 privateKeyJwk
      5. 将加密结果（ciphertext + nonce）存入 `user_key_backups` 表
      6. 记录审计日志到 `audit_logs` 表
    - **返回**：
      ```typescript
      {
        success: boolean,
        keyVersion: number,
        backedUpAt: string  // ISO timestamp
      }
      ```
    - **错误处理**：
      - 401: 未登录或 JWT 无效
      - 400: privateKeyJwk 格式错误
      - 500: Master Key 缺失或加密失败

    **5.3.2 恢复 API: `POST /key-restore`**
    - **功能**：解密用户的备份私钥并返回给前端
    - **入参**：
      ```typescript
      {
        keyVersion?: number  // 可选，默认恢复最新版本
      }
      ```
    - **处理流程**：
      1. 验证 Supabase JWT，获取 user_id
      2. 从 `user_key_backups` 查询对应版本的备份记录
      3. 从环境变量读取 Master Key
      4. 使用 AES-GCM 解密 encrypted_private_key
      5. 返回解密后的 JWK 给前端
      6. 记录审计日志（包含 IP、设备信息）
    - **返回**：
      ```typescript
      {
        privateKeyJwk: JsonWebKey,
        keyVersion: number,
        backedUpAt: string
      }
      ```
    - **错误处理**：
      - 401: 未登录
      - 404: 备份记录不存在
      - 500: 解密失败（Master Key 错误或数据损坏）

    **5.3.3 Master Key 加密/解密工具函数**
    ```typescript
    // supabase/functions/_shared/masterKeyUtils.ts
    const MASTER_KEY_ENV = 'MASTER_KEY_BASE64';

    async function getMasterKey(): Promise<CryptoKey> {
      const base64Key = Deno.env.get(MASTER_KEY_ENV);
      if (!base64Key) {
        throw new Error('Master key not configured');
      }
      const keyData = base64ToBytes(base64Key);
      return await crypto.subtle.importKey(
        'raw',
        keyData,
        { name: 'AES-GCM' },
        false,
        ['encrypt', 'decrypt']
      );
    }

    async function encryptWithMasterKey(
      plaintext: string
    ): Promise<{ ciphertext: string; nonce: string }> {
      const masterKey = await getMasterKey();
      const nonce = crypto.getRandomValues(new Uint8Array(12));
      const encoder = new TextEncoder();
      const data = encoder.encode(plaintext);

      const encrypted = await crypto.subtle.encrypt(
        { name: 'AES-GCM', iv: nonce },
        masterKey,
        data
      );

      return {
        ciphertext: bytesToBase64(new Uint8Array(encrypted)),
    ytesToBase64(nonce)
      };
    }

    async function decryptWithMasterKey(
      ciphertext: string,
      nonce: string
    ): Promise<string> {
      const masterKey = await getMasterKey();
      const cipherBytes = base64ToBytes(ciphertext);
      const nonceBytes = base64ToBytes(nonce);

      const decrypted = await crypto.subtle.decrypt(
        { name: 'AES-GCM', iv: nonceBytes },
        masterKey,
        cipherBytes
      );

      const decoder = new TextDecoder();
      return decoder.decode(decrypted);
    }
    ```

    **5.4 前端集成**

    **5.4.1 自动备份触发点**
    - **时机 1**：用户首次生成密钥对后（在 `ensureUserKeyPair` 中）
    - **时机 2**：密钥轮换后（未来功能）
    - **实现位置**：`web/src/services/cryptoKeyService.ts`

    **5.4.2 备份函数实现**
    ```typescript
    // web/src/services/cryptoKeyService.ts
    export const backupUserPrivateKey = async (
      user: User,
      privateKey: CryptoKey
    ): Promise<void> => {
      if (!isWebCryptoAvailable()) {
        return;
      }

      try {
        // 导出私钥为 JWK 格式
        const privateKeyJwk = await crypto.subtle.exportKey('jwk', privateKey);

        // 调用后端备份 API
        const { data, error } = await supabase.functions.invoke('key-backup', {
          body: {
            privateKeyJwk,
            keyVersion: 1
          }
        });

        if (error) {
          console.error('Failed to backup private key:', error);
          // 不阻断主流程，仅记录错误
          return;
        }

        console.log('Private key backed up successfully:', data);
      } catch (err) {
        console.error('Backup error:', err);
        // 静默失败，不影响用户体验
      }
    };
    ```

    **5.4.3 恢复函数实现**
    ```typescript
    // web/src/services/cryptoKeyService.ts
    export const restoreUserPrivateKey = async (
      user: User
    ): Promise<CryptoKey | null> => {
      if (!isWebCryptoAvailable()) {
        return null;
      }

      try {
        // 调用后端恢复 API
        const { data, error } = await supabase.functions.invoke('key-restore', {
          body: {}
        });

        if (error || !data?.privateKeyJwk) {
          console.warn('No backup found or restore failed:', error);
          return null;
        }

        // 导入恢复的私钥
        const privateKey = await crypto.subtle.importKey(
          'jwk',
          data.privateKeyJwk,
          {
            name: 'RSA-OAEP',
            hash: 'SHA-256'
          },
          false,  // non-exportable
          ['decrypt']
        );

        // 同时恢复公钥（从 JWK 派生）
        const publicKey = await crypto.subtle.importKey(
          'jwk',
          {
            ...data.privateKeyJwk,
            d: undefined,
            dp: undefined,
            dq: undefined,
            p: undefined,
            q: undefined,
            qi: undefined
          },
          {
            name: 'RSA-OAEP',
            hash: 'SHA-256'
          },
          true,
          ['encrypt']
        );

        // 保存到 IndexedDB
        await saveKeyPair(user.id, { publicKey, privateKey });

        return privateKey;
      } catch (err) {
        console.error('Restore error:', err);
        return null;
      }
    };
    ```

    **5.4.4 集成到密钥初始化流程**
    ```typescript
    // 修改 ensureUserKeyPair 函数
    export const ensureUserKeyPair = async (user: User): Promise<void> => {
      if (!isWebCryptoAvailable()) {
        return;
      }

      try {
        let stored = await getStoredKeyPair(user.id);

        // 如果本地没有密钥，尝试从备份恢复
        if (!stored) {
          const restoredPrivateKey = await restoreUserPrivateKey(user);
          if (restoredPrivateKey) {
            stored = await getStoredKeyPair(user.id);
            console.log('Private key restored from backup');
          }
        }

        // 如果恢复失败或无备份，生成新密钥对
        if (!stored) {
          const keyPair = await crypto.subtle.generateKey(
            {
              name: 'RSA-OAEP',
              modulusLength: 2048,
              publicExponent: new Uint8Array([1, 0, 1]),
              hash: 'SHA-256'
            },
            true,
            ['encrypt', 'decrypt']
          ) as CryptoKeyPair;

          await saveKeyPair(user.id, keyPair);
          stored = {
            publicKey: keyPair.publicKey,
            privateKey: keyPair.privateKey
          };

          // 自动备份新生成的私钥
          await backupUserPrivateKey(user, keyPair.privateKey);
        }

        // 上传公钥到 users_profile
        const publicJwk = await crypto.subtle.exportKey('jwk', stored.publicKey);
        await upsertUserPublicKey(user, publicJwk);
      } catch (err) {
        console.error('Key initialization error:', err);
      }
    };
    ```

    **5.5 用户体验优化**

    **5.5.1 恢复提示 UI**
    - 在新设备登录后，如果检测到本地无密钥但有备份，显示提示：
      ```
      “检测到您的密钥备份，正在自动恢复...”
      ```
    - 恢复成功后：
      ```
      “密钥恢复成功，您可以正常访问加密文档”
      ```
    - 恢复失败时：
      ```
      “密钥恢复失败，您可能无法访问之前的加密文档。请联系支持团队。”
      ```

    **5.5.2 备份状态指示**
    - 在用户设置页面显示备份状态：
      - ✅ 已备份（最后备份时间：2024-03-16 10:30）
      - ⚠️ 未备份（建议立即备份）
    - 提供”立即备份”按钮（手动触发备份）

    **5.6 安全加固**

    **5.6.1 审计日志**
    - 在 `audit_logs` 表记录以下操作：
      - `key_backup_created`: 密钥备份创建
      - `key_restore_requested`: 密钥恢复请求
      - `key_restore_success`: 恢复成功
      - `key_restore_failed`: 恢复失败
    - 记录字段：user_id, action, ip, user_agent, created_at

    **5.6.2 频率限制**
    - 恢复 API 限制：每用户每小时最多 5 次请求
    - 备份 API 限制：每用户每天最多 10 次请求
    - 使用 Supabase Edge Function 的 rate limiting 或自定义实现

    **5.6.3 敏感操作通知**
    - 密钥恢复成功后，发送邮件通知用户：
      ```
      您的账号在新设备上恢复了密钥
      时间：2024-03-16 10:30
      IP：192.168.1.1
      设备：Chrome on macOS

      如果不是您本人操作，请立即修改密码并联系支持团队。
      ```

    **5.7 阶段验证**
    - **功能测试**：
      1. 新用户注册 → 生成密钥 → 自动备份 → 检查 `user_key_backups` 表有记录
      2. 清除浏览器数据 → 重新登录 → 自动恢复 → 验证可解密旧文档
      3. 手动触发备份 → 检查版本号递增
    - **安全测试**：
      1. 尝试直接访问 `user_key_backups` 表 → 应无法解密
      2. 尝试伪造 JWT 调用恢复 API → 应返回 401
      3. 频繁调用恢复 API → 应触发频率限制
    - **异常测试**：
      1. Master Key 配置错误 → 备份/恢复应返回明确错误
      2. 备份记录不存在 → 恢复应返回 404 并提示用户
      3. 网络中断 → 备份失败不应阻断密钥生成流程

    **5.8 文档与配置**
    - 在 `CLAUDE.md` 中补充密钥备份架构说明
    - 在 `部署指南.md` 中添加 Master Key 配置步骤
    - 在用户协议中明确说明平台托管模式的安全边界
  - [ ] **子任务 6：兼容与迁移策略**
    - 浏览器兼容性：探测 Web Crypto 支持情况，不支持时给出明确提示（只读模式或禁用加密功能），避免出现“看似成功但实际未加密”的状态。
    - 旧数据迁移：为目前仍然存储在 Supabase Storage 的明文文档设计渐进式迁移策略（例如：首次打开旧文档时提示“升级为加密文档”，重新保存生成加密版本）。
    - 定义“加密标记”：在 `documents`/`document_versions` 或 `encrypted_meta` 中增加标志位，用于区分已加密与未加密文档，并确保前端逻辑兼容两种状态。
  - [ ] **子任务 7：安全基线与阶段验证**
    - 禁止上报 URL hash、任何密钥材料及敏感密文头部到日志/埋点，审查现有埋点与日志打印。
    - 补充单元测试与集成测试：覆盖加密/解密的正确性、MAC 校验失败、密文被篡改、密钥缺失等异常路径；结合“阶段验证（路径A）”整体用例完成端到端回归。
    - 将依赖安全审计、依赖升级策略与 RLS 回归测试纳入发布检查清单，确保端到端加密上线后不会因依赖或配置问题削弱安全性。
- [ ] **版本记录与保留（只读展示，仅载入最新）**：
  - 版本生成：
    - 每次点击“保存”并确认后，生成一个新的版本记录（document\_versions），并在卡片的版本下拉中只读展示（版本号、备注、作者、时间；最新在最上）。
  - 版本使用：
    - UI 不提供“查看/载入历史版本”的交互入口；文档卡片“载入”始终加载最新版本。
  - 版本信息长期保留（默认）：
    - 版本下拉默认展示该文档全部历史版本记录（不设置 30 天过期规则）。
  - 存储成本控制（可选）：
    - 由于 UI 永远只载入最新版本，可仅保证“最新版本”的 R2 对象可用；历史版本可只保留版本元数据（版本号/备注/作者/时间/大小等），不保证其 R2 内容可回溯下载。
- [ ] **邀请制共享与角色管理（用户/用户组）**：
  - 共享管理页：
    - 邀请用户：检索用户、发送邀请、设置角色（reader/editor/inviter/admin）。
    - **前端密钥封装 (Key Wrapping)**：
      - 在邀请操作中，前端获取被邀请人的公钥 (`public_key` from `users_profile`)。
      - 使用该公钥加密当前文档的 `DocumentKey`，生成 `wrapped_key`。
      - 将 `wrapped_key` 连同授权信息写入 `document_keys` / `document_members` 表。
    - 邀请用户组：给组授予角色，并确保组内每个用户都能获得自己的 wrapped\_document\_key。
    - 角色调整：仅 admin 可调整成员角色；inviter 仅可新增邀请（按策略限制）。
    - 撤销访问：移除成员/移除组授权；撤销后禁止生成签名 URL；必要时支持触发密钥轮换（后续增强）。
  - 用户组管理页：
    - 创建组、改名、删除（软删）。
    - 添加/移除成员、设置组内角色。
    - 支持按组快速授权文档。
- [ ] **前端界面与交互（共享文档页签）**：
  - 顶部栏按钮：
    - 在右上角头像左侧新增“文档列表”按钮。
    - “保存”按钮放置在“导出文档”按钮左侧；默认不显示，仅在导入或载入文档后显示。
  - 文档列表浮层（右侧滑出 Drawer）：
    - 点击“文档列表”按钮，从右侧滑出文档列表浮层。
    - 浮层内使用滑块页签（Tabs）实现两部分切换：默认“个人文档”，以及“共享文档”。
    - 切换到“共享文档”页签后展示“共享文档”列表。
  - **数据集成 (Data Integration)**：
    - 扩展 `DocumentService`。
    - 封装联表查询逻辑：通过 `document_members` 或 `document_group_members` 获取当前用户被授权的文档。
  - 共享文档卡片（卡片展示）：
    - 基础信息展示：文档名称、版本号、时间、作者、备注、大小。
    - 操作按钮：仅提供“载入”按钮（不提供“共享/取消共享”按钮）。
    - “版本号”交互：
      - 卡片默认显示最新版本号。
      - 通过版本号下拉展示该文档所有历史版本信息：版本号、备注、作者、时间；最新版本在最上方，依次排列。
      - 版本下拉菜单仅做只读展示，不提供任何交互操作入口。
  - “载入”交互：
    - 点击“载入”将对应共享文档的最新版本加载到文档预览区（不支持载入历史版本）。
    - 载入后同步更新左侧目录区（复用现有解析/目录生成能力）。
  - “保存”交互（手动触发保存当前共享文档为新版本）：
    - 点击“保存”按钮，弹出“保存”弹窗：
      - 版本号输入框：系统自动填充默认值（例如 V1.0.0），用户可修改后再保存。
      - 备注输入框：用于填写本次更新信息，默认为空。
      - 确认保存后才会生成新版本并更新“共享文档”列表记录；取消/关闭弹窗不产生变更。
    - **保存流程实现**：
      - 复用个人文档的保存流程 (Blob -> Encrypt -> Upload -> DB)。
      - 增加权限检查：确保当前用户对该共享文档拥有 `editor` 或 `admin` 权限。
    - 当前会话源自“共享文档”：保存到“共享文档”页签下（在权限允许时创建新版本）。
    - 未点击“保存”按钮时：文档在预览/编辑过程中的任何变更均不会写入文档列表记录（不自动保存）。
  - 预览区标题标识：
    - 共享文档载入后：在预览区顶部文档标题后显示“（共享）”，绿色字体。
  - 阶段验证：
    - 功能测试：Tabs 切换到“共享文档”后列表正常展示；共享文档卡片不显示“共享/取消共享”按钮。
    - 功能测试：载入后预览与目录同步更新，且标题显示“（共享）”标识。
    - 功能测试：保存弹窗默认值与取消行为正确；不点击保存不更新“共享文档”列表记录。
- [ ] **阶段验证（路径A）**：
  - 单人：上传加密版本 → 多次保存生成版本 → 版本下拉可见全部版本（只读）→ 载入始终为最新版本 → 导出文档。
  - 共享（用户）：邀请用户为 reader/editor → 被邀请人可解密查看/或编辑并上传版本（权限生效）。
  - 共享（用户组）：给组授权后组内成员可访问；移除组授权后访问立即失效。
  - 权限与泄露验证：
    - 非成员无法获取预签名 URL。
    - 服务端数据库与 R2 存储均不出现明文文档内容。
    - 非最新版本内容不提供回溯下载能力（仅保留版本元数据用于只读展示）。

## 9. 第八阶段：安全、审计与可运维性 (Phase 8: Security, Audit & Ops)

- [ ] **审计与风控（可选但推荐）**：
  - 记录关键动作：邀请、改角色、撤销、下载、上传、删除、生成签名 URL。
  - 为异常行为预留限制点：频率限制、批量下载告警、IP 异常等。
- [ ] **一致性与幂等**：
  - 处理“DB 写入成功但 R2 上传失败”“R2 删除失败但 DB 删除成功”等边界，保证可重试且不会产生幽灵数据。
  - 对清理任务实现幂等（重复执行不产生副作用）。
- [ ] **发布检查清单**：
  - 前端不输出敏感信息（密钥/密文头部不包含可识别明文元数据）。
  - RLS 策略回归测试通过（最小权限原则）。
  - 依赖漏洞扫描与升级策略确认。
