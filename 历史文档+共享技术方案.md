# 历史文档与共享技术方案（路径A：Supabase 元数据 + R2 大文件 + 客户端加密）

**目标**
- 历史文档查看：查看版本列表、版本详情、对比基础信息（作者、时间、备注）
- 载入编辑：从历史版本载入到当前编辑会话（预览/继续编辑/导出新版本）
- 多人共享：邀请制共享（指定用户/用户组），角色为只读/可编辑/可转发/可管理
- 隐私：服务端无法看到文档内容（客户端加密存储，端到端加密）
- 保留策略：文档版本最多保留 30 天（过期自动清理）

**约束与定位**
- 路径A不引入“实时协同编辑（CRDT/Y.js + WebSocket）”作为首要目标，优先落地“共享 + 版本历史 + 载入编辑”
- 文档内容加密后存储于 R2；Supabase 只存储加密元数据与访问控制数据
- 当前项目的 DOCX 解析/预览/导出逻辑仍在客户端执行，因此客户端在解密后可进行解析与渲染

## 架构与组件

**总体架构**
- 客户端（浏览器）：加解密、DOCX 解析预览、导出/生成版本、权限判断与 UI
- Supabase（Auth + Postgres + RLS）：用户体系、用户组、文档/版本元数据、分享关系、密钥分发（加密后）、审计与限流辅助
- Cloudflare R2：仅存储加密后的文档二进制（EncryptedBlob）
- 签名服务（生成 R2 预签名 URL）：建议使用 Supabase Edge Function（或 Cloudflare Worker）实现

**为什么 Web Crypto API 在路径A可用**
- 可用：对“文件内容/版本快照”进行客户端加密后上传到 R2；服务端不掌握明文与解密密钥
- 代价：服务端无法做全文检索、内容审查、服务端预览、基于内容的增量协同
- 适用性：你当前项目已经在客户端做 DOCX 解析/导出，天然匹配“解密后本地处理”

## 端到端加密（E2EE）设计（路径A版）

### 密钥层级
- 文档密钥（DocumentKey）：每个文档一把对称密钥，用于加密该文档所有版本的内容（或用于派生每版本密钥）
- 版本密钥（VersionKey，可选）：从 DocumentKey 派生（HKDF），用于不同版本采用不同密钥，便于未来做密钥轮换与撤销影响面控制
- 用户密钥对（UserKeyPair）：每个用户一对公私钥，用于“邀请制共享下的密钥分发（对 DocumentKey 进行包裹/加密）”

### 算法建议
- 内容加密：AES-GCM（256bit）
- 密钥分发：X25519（密钥交换）+ HKDF + AES-GCM，或 RSA-OAEP（工程实现更简单但密文较大）
- 哈希：SHA-256（用于完整性校验、去重与一致性检查）

### 用户密钥对生成与存储
- 生成位置：首次登录后在浏览器生成 UserKeyPair
- 公钥：存入 Supabase profile 表（可公开）
- 私钥：仅存本地（IndexedDB），用用户口令派生密钥加密后保存（避免明文落盘）

### 邀请制共享下的密钥分发（核心）
- 创建文档时：客户端生成 DocumentKey；同时写入一条“拥有者的密钥包裹记录”
- 邀请成员时：客户端读取被邀请人公钥，将 DocumentKey 加密（或 ECDH 派生会话密钥后包裹）并写入服务端
- 成员访问时：客户端拉取属于自己的加密 DocumentKey，使用本地私钥解密得到 DocumentKey，从而解密 R2 上的内容

### 分块加密与大文件处理
- 由于 Web Crypto 原生不提供真正的流式 AES-GCM，建议采用“分块加密”
- 每块一个 nonce（12 bytes），nonce = random(8) + chunkIndex(4) 或 random(12) 并保证不重复
- EncryptedBlob 格式建议：
  - header：版本、算法、chunkSize、chunks、metaHash
  - chunks：每块密文 + tag
- 优点：支持断点续传、并发上传、部分重传与校验

## 数据模型（Supabase Postgres）

### 基础表（建议）
1. users_profile
   - user_id（引用 auth.users）
   - display_name
   - public_key（用于密钥分发）
2. groups
   - id, name, owner_user_id, created_at
3. group_members
   - group_id, user_id, role（group_owner/group_admin/member）
4. documents
   - id, created_by, created_at
   - encrypted_title（可选）
   - deleted_at（软删除）
5. document_members（按用户授权）
   - document_id, user_id
   - role（reader/editor/inviter/admin）
   - status（invited/active/removed）
6. document_group_members（按用户组授权）
   - document_id, group_id
   - role（reader/editor/inviter/admin）
7. document_keys（密钥包裹记录，一人一份）
   - document_id, user_id
   - wrapped_document_key（加密后的 DocumentKey）
   - key_version（用于轮换）
8. document_versions
   - id, document_id, created_at, created_by
   - r2_key（R2 对象键）
   - content_hash（SHA-256）
   - encrypted_meta（备注/文件名等）
   - size_bytes
   - expires_at（created_at + 30 days）
9. audit_logs（可选）
   - actor_user_id, document_id, action, created_at, ip, user_agent

### RLS 核心要点（必须）
- documents：仅成员可见；admin 可管理成员；inviter 可邀请；editor 可写入版本；reader 只读
- document_versions：仅成员可见；写入需 editor/admin；删除需 admin；读取需 reader+
- document_keys：仅 owner（user_id = auth.uid()）可读写自己的 wrapped key
- group_members：组内可见；组 owner/admin 可改成员

## 关键业务流程

### 1）创建文档
- 客户端：生成 DocumentKey（与可选的 VersionKey 派生策略）
- 客户端：写入 documents（可写入 encrypted_title）
- 客户端：为创建者写入 document_members（role=admin）与 document_keys（wrapped_document_key）

### 2）上传新版本（保存/导出新版本）
- 客户端：基于 DocumentKey（或派生 VersionKey）分块加密原始 docx/导出文件
- 客户端：请求“签名服务”获取 R2 预签名 PUT URL（绑定 document_id、version_id、对象 key）
- 客户端：直传 R2（EncryptedBlob）
- 客户端：写入 document_versions（r2_key、hash、size、expires_at、encrypted_meta）

### 3）查看历史版本列表
- 客户端：查询 document_versions（按 created_at 倒序，过滤 expires_at > now）
- UI：展示时间、作者、备注（encrypted_meta 需先解密后展示；或备注不加密但会泄露信息）

### 4）载入历史版本进行预览/编辑
- 客户端：获取该版本 r2_key 的预签名 GET URL
- 客户端：下载 EncryptedBlob → 解密 → 得到 docx/内容 → 使用现有解析逻辑渲染到预览区/编辑器
- 客户端：如需要“继续编辑”并保存，保存时生成新版本（不修改旧版本）

### 5）邀请共享（指定用户）
- 前提：邀请者具备 inviter/admin 权限
- 邀请流程：
  - 客户端：读取被邀请人 public_key
  - 客户端：加密 DocumentKey 写入 document_keys（user_id=被邀请人）
  - 服务端：写入 document_members（status=invited/active；可用邮件通知或站内通知）

### 6）用户组共享
- 创建组 → 添加成员 → 给组授予文档角色（document_group_members）
- 解密密钥分发策略：
  - 推荐：仍然按“用户粒度”写入 document_keys（组内每个用户一份 wrapped key），避免“组密钥泄露导致组内全员不可控”
  - 当用户加入组且组对某文档有权限时，由文档管理员触发或客户端批量补发该用户的 wrapped key

### 7）角色定义（建议）
- reader（只读）：可查看文档与版本、可载入预览，不可上传版本、不可邀请
- editor（可编辑）：在 reader 基础上可上传新版本
- inviter（可转发）：在 reader 基础上可邀请用户/用户组（不一定允许改角色）
- admin（可管理）：成员与角色管理、撤销访问、删除版本/文档、触发密钥轮换

## 30 天保留与清理策略

**规则**
- document_versions.expires_at = created_at + 30 days
- 超期版本不可见（UI 与查询层过滤），并由后台任务清理 R2 对象与 DB 记录

**清理实现建议（任选其一）**
- Supabase 定时任务（如果可用）：每天运行一次 SQL/函数，找出 expires_at < now() 的版本，按批次删除并记录审计
- Edge Function 定时触发：拉取过期列表 → 调用 R2 删除对象 → 删除/标记 DB 记录

## R2 预签名与安全

**预签名 URL 原则**
- 最小权限：PUT 只允许写入指定 r2_key；GET 只允许读取指定 r2_key
- 短时效：建议 60～300 秒
- 绑定鉴权：签名服务必须校验 Supabase JWT + RLS 权限（reader/editor/admin）
- 防重放：可加入一次性 token/nonce（存 Supabase，使用后失效）

## UI/功能清单（路径A）
- 历史版本面板：列表（时间/作者/备注/大小）、查看、载入、删除（admin）
- 当前文档：上传新版本（editor+）、导出新版本（editor+）
- 共享管理（admin/inviter）：邀请用户、邀请用户组、角色修改、移除成员、查看审计
- 用户组：创建组、加人、改组内角色、对组授予文档权限

## 风险与边界
- E2EE 下服务端无法预览内容与全文检索；若未来要搜索，只能做“客户端本地搜索”或“加密可搜索（代价极高）”
- 私钥丢失会导致无法解密历史内容：需要“密钥备份/恢复”机制（例如用户口令导出恢复包，或多设备同步但仍保持端到端加密）
- 共享撤销后，已下载的旧密文仍可能被离线保存：撤销主要限制“未来的在线访问与签名下载”，并建议密钥轮换降低持续风险
